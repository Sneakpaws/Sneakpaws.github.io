/*
 * index.js
 */

document.addEventListener("DOMContentLoaded", () => {
    // Initialize carousels
    const headshots = new bootstrap.Carousel(document.querySelector("#headshots"));
    const halfbodies = new bootstrap.Carousel(document.querySelector("#halfbodies"));
    const fullbodies = new bootstrap.Carousel(document.querySelector("#fullbodies"));
    const scenes = new bootstrap.Carousel(document.querySelector("#scenes"));
    const basicRefs = new bootstrap.Carousel(document.querySelector("#basicRefs"));
    const detailedRefs = new bootsrap.Carousel(document.querySelector("#detailedRefs"));
    const traditional = new bootstrap.Carousel(document.querySelector("#traditional"));
});
